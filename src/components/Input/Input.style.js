import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    margin: 5,
    backgroundColor: 'white',
    borderRadius: 10,
    flexDirection: 'row',
  },
  input: {
    flex: 1,
  },
});
